using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IniciarDesafio : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
        StoryEvents.GatilhoDesafio();
    }
}
